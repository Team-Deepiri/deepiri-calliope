# Netlify

Configure a new Netlify site with this repository. The build installs frontend dependencies and runs `vite build`. Environment variables for API calls should point the SPA at your deployed API (update fetch base URL or add a build-time `VITE_API_BASE` if you split hosts).
