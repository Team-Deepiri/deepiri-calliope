# Ollama

The `deepiri-ollama-utils` package supplies connectivity checks used by the `/v1/ollama/status` route. Long-form generation uses the standard `/api/generate` HTTP API with a music-specific system prompt layered in `calliope.services.prompts`.
